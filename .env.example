# Copy to .env and fill in. .env is gitignored; this file is not.

# Contact address sent in the User-Agent to Overpass / Nominatim, as their
# usage policies require (ingestion/contact.py). Calls fail loudly if unset.
GEOWATCH_CONTACT_EMAIL=you@example.org
